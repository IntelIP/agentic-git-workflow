name: Tabellio Evidence

on:
  pull_request:

permissions:
  contents: read
  actions: read

jobs:
  evidence:
    # Native context capture is unreleased. Pin both refs to its release tag or commit SHA for production.
    uses: IntelIP/Tabellio/.github/workflows/tabellio-evidence.yml@main
    with:
      # Replace with the repository's normal validation command.
      validation_command: npm test
      toolkit_ref: main
